/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactStrictMode: true,
  images: {
    domains: ['e0.365dm.com', 'static.cricbuzz.com', 'crickettimes.com', 'www.probatsman.com', 'fitness-nation.net']
  }
};

export default nextConfig;
